#include<stdio.h>
void main()
{
	int n,r,sum,temp;
	printf("enter the numbers=");
	scanf("%d%d",&a,&b);
	temp=n;
	while(n>0)
	
    {
    	r=n%10;
    	sum=(sum*10)+r;
    	n=n/10;
	}
	if(temp==sum)
	printf(the given number is palindrome number");
	e;
}
